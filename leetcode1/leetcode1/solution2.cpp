/*
#include <cstdio>
#include <cstdlib>
#include <vector>
using namespace std;
struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(NULL), right(NULL) {}
};

class Solution {
public:
    string smallestFromLeaf(TreeNode* root) {
		vector<vector<int>> tmpstr;
		TreeNode* tmp = root;
		while(tmp->left != NULL || tmp->right != NULL)
		{

		}

		

    }
	void find(TreeNode* root, vector<int>* str)
	{
		if(root->left != NULL)
	}

};*/